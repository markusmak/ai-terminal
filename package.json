{
  "name": "ai-terminal",
  "version": "1.0.0",
  "description": "Learning by building a command line tool that takes in natural language instructions and executes the instructions.",
  "main": "test.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@langchain/openai": "^0.0.11",
    "langchain": "^0.1.2"
  }
}
